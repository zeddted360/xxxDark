
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dark Bonds | support",
  description: "Tested and Trusted",
};

import React from "react";
import ChatPage from "./Support";

export default function page() {
  return (
      <ChatPage />
  );
}
